const time = new Date()
const hour = time.getHours()
const mins = time.getMinutes()

console.log('The current time is ' + hour + ':' + mins)

// app.style.setProperty('--_dm', `${mins}s`)
// app.style.setProperty('--_dh', `${hour + mins}s`)
